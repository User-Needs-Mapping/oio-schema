{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/User-Needs-Mapping/oio-schema/schemas/versions/oio-schema-1.0.0.json",
  "title": "Outside-in Org Data Model",
  "description": "Frozen OIO 1.0.0 structural contract. Validates users, needs, capabilities, technical systems, teams and their relationships, and nothing else. Kept so a consumer can select the structural-only contract explicitly rather than inferring it. New work targets schemas/oio-schema.json.",
  "type": "object",
  "required": [
    "system"
  ],
  "additionalProperties": false,
  "properties": {
    "system": {
      "title": "System",
      "description": "What this model is about, and where its edges are. Everything else in the document is understood to sit inside the boundary named here.",
      "$ref": "#/definitions/system"
    },
    "users": {
      "title": "Users",
      "description": "The people the organisation serves, and the other systems that depend on it. Add a separate user only where the goals genuinely differ: two names for people who want the same thing are one user.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/user"
      }
    },
    "needs": {
      "title": "Needs",
      "description": "What each user is trying to accomplish. Needs bridge users to capabilities.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/need"
      }
    },
    "capabilities": {
      "title": "Capabilities",
      "description": "The abilities the organisation needs in order to meet those needs. Capabilities are the heart of an outside-in model: they outlast the teams that staff them and the software that implements them, so name them for what can be done, never for who does it.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/capability"
      }
    },
    "technical_systems": {
      "title": "Technical Systems",
      "description": "The software and services that actually deliver capabilities. Each one lists the capabilities it stands behind and the outside systems it leans on.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/technicalSystem"
      }
    },
    "teams": {
      "title": "Teams",
      "description": "Organizational units that steward capabilities and technical systems. Classified using the Team Topologies pattern language.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/team"
      }
    },
    "org_groups": {
      "title": "Organisational Groups",
      "description": "Optional grouping of teams into larger organizational units (value streams, platforms, tribes, domains, org-units).",
      "type": "array",
      "items": {
        "$ref": "#/definitions/orgGroup"
      }
    },
    "data_assets": {
      "title": "Data Assets",
      "description": "Stores and channels that more than one technical system touches. Worth recording precisely because two systems reaching into the same store are coupled whether or not anyone meant them to be.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/dataAsset"
      }
    },
    "external_dependencies": {
      "title": "External Dependencies",
      "description": "Things the organisation relies on but does not run. Declare each one here; which technical systems actually call it is stated on those systems, under externalDeps.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/externalDependency"
      }
    }
  },
  "definitions": {
    "system": {
      "title": "System",
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "System Name",
          "description": "What the modelled system is called. One model, one name: it is how a reader tells this document apart from a neighbouring one.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "description": {
          "title": "System Description",
          "description": "What this system is for, and what falls inside its boundary. Say what is deliberately out of scope too — a reader cannot tell an omission from an oversight.",
          "type": "string",
          "maxLength": 2000
        },
        "version": {
          "title": "Version",
          "description": "User-controlled version identifier for this model's content. Set automatically by the platform on export. This is not the OIO schema version — see oio_version.",
          "type": "string",
          "maxLength": 50
        },
        "lastModified": {
          "title": "Last Modified",
          "description": "When the document was last written out, as an ISO 8601 timestamp. Producing tools set this; an author writing by hand can leave it out.",
          "type": "string",
          "maxLength": 50
        },
        "author": {
          "title": "Author",
          "description": "Whoever last changed or exported the document. This is authorship of the file, not of the organisation it describes, and not provenance for any record inside it.",
          "type": "string",
          "maxLength": 200
        }
      }
    },
    "user": {
      "title": "User",
      "description": "Someone the organisation serves, or another system that depends on it. Model a separate user only where the goals genuinely differ: two labels for people who want the same thing are one user.",
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "User Name",
          "description": "What this user is called. Unique across users, and the exact string a need uses to point back here.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "type": {
          "title": "User Type",
          "description": "An optional free-text label for the kind of user this is. There is no controlled list: use the word the organisation already uses, and expect it to differ from the next organisation's.",
          "type": "string",
          "maxLength": 200
        },
        "description": {
          "title": "User Description",
          "description": "Who this user is, and what they come to the organisation for.",
          "type": "string",
          "maxLength": 2000
        }
      }
    },
    "need": {
      "title": "Need",
      "description": "What a user (or multiple users) is trying to accomplish. Needs bridge users to capabilities.",
      "type": "object",
      "required": [
        "name",
        "user"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Need Name",
          "description": "What this need is called. No two needs may share a name, because structural references resolve by name.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "user": {
          "title": "User",
          "description": "Whose need this is. Give one user name, or a list where several users share it.",
          "oneOf": [
            {
              "type": "string",
              "minLength": 1,
              "maxLength": 200
            },
            {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1,
                "maxLength": 200
              },
              "minItems": 1
            }
          ]
        },
        "outcome": {
          "title": "Outcome",
          "description": "What success looks like, written in the user's own voice. This is a sentence, not a reference: it keeps exactly the meaning it had in OIO 1.0 and is never an id of an entry in the `outcomes` section.",
          "type": "string",
          "maxLength": 2000
        },
        "fulfilledBy": {
          "title": "Fulfilled By",
          "description": "The capabilities that meet this need. Write a bare capability name where nothing more is needed, or a relationship object where the link itself deserves a role, a label or an identifier.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          },
          "minItems": 1
        },
        "decomposesInto": {
          "title": "Decomposes Into",
          "description": "Finer-grained needs this one breaks down into. Give a bare need name, or a relationship object where the link needs a role, a label or an identifier.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        }
      }
    },
    "capability": {
      "title": "Capability",
      "description": "Something the organisation must be able to do. A capability is an ability, not the team that holds it and not the system that implements it: both of those can be replaced without the capability changing.",
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Capability Name",
          "description": "What this capability is called. Names are unique across capabilities, since other sections point at them by name.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "description": {
          "title": "Capability Description",
          "description": "What the organisation is able to do when this capability is present. Describe the ability, not the team or the software that happens to supply it today.",
          "type": "string",
          "maxLength": 2000
        },
        "visibility": {
          "title": "Visibility",
          "description": "How far from the user this capability sits. user-facing: the user meets it directly; domain: the business rules and processing behind that; foundational: shared internals the domain layer leans on; infrastructure: the platform underneath everything.",
          "type": "string",
          "enum": [
            "user-facing",
            "domain",
            "foundational",
            "infrastructure"
          ]
        },
        "evolution_stage": {
          "title": "Evolution Stage",
          "description": "How evolved this capability is as a practice in the wider world, in the Wardley sense: genesis: novel and uncertain; custom-built: built to fit, still changing; product: a well-understood product or rental market exists; commodity: standardised and utility-like. This axis describes ubiquity and certainty of the practice. It says nothing about how well this organisation performs the capability, how mature its implementation is, or whether it is being retired.",
          "type": "string",
          "enum": [
            "genesis",
            "custom-built",
            "product",
            "commodity"
          ]
        },
        "strategic_importance": {
          "title": "Strategic Importance",
          "description": "The strategic importance of this capability to the business. core: essential to the core value proposition; supporting: contributes to competitive differentiation; generic: necessary but not a key differentiator.",
          "type": "string",
          "enum": [
            "core",
            "supporting",
            "generic"
          ]
        },
        "dependsOn": {
          "title": "Depends On",
          "description": "Capabilities this one cannot work without. A bare name is enough; use a relationship object where the dependency needs a role, a label or an identifier of its own.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        },
        "decomposesInto": {
          "title": "Decomposes Into",
          "description": "Finer-grained capabilities this one breaks down into. Give a bare capability name, or a relationship object where the link needs a role, a label or an identifier.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        }
      }
    },
    "technicalSystem": {
      "title": "Technical System",
      "description": "A running system that delivers one or more capabilities. Which team looks after it is recorded on the team, under stewards, not here.",
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Technical System Name",
          "description": "The name of the technical system. Must be unique within the model.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "description": {
          "title": "Technical System Description",
          "description": "A description of what this technical system does.",
          "type": "string",
          "maxLength": 2000
        },
        "enables": {
          "title": "Enables",
          "description": "The capabilities this system delivers. Declared here and only here: a capability never names the systems behind it. Give a bare capability name, or a relationship object to add a role or a label.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        },
        "constrains": {
          "title": "Constrains",
          "description": "Capabilities this system holds back. Declared here and only here: a capability never names what constrains it. Give a bare capability name, or a relationship object to add a role or a label. This is a structural statement about an implementation limiting a capability, and is not the same thing as an entry in the `constraints` section.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        },
        "dependsOn": {
          "title": "Depends On",
          "description": "Other technical systems this technical system calls. Each entry is a technical system name or a relationship object.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        },
        "externalDeps": {
          "title": "External Dependencies",
          "description": "Outside systems this one calls. Every name here has to appear in external_dependencies.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          }
        }
      }
    },
    "team": {
      "title": "Team",
      "description": "An organizational unit that stewards capabilities and technical systems. Classified using the Team Topologies pattern language.",
      "type": "object",
      "required": [
        "name",
        "type"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Team Name",
          "description": "What this team is called. Names are unique across teams, and are how interactions and stewardship point at them.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "type": {
          "title": "Team Type",
          "description": "Which of the four Team Topologies types this team is. stream-aligned: carries a flow of work for one slice of the business; platform: provides services that let stream-aligned teams go faster on their own; enabling: helps other teams learn something and then steps back; complicated-subsystem: holds specialist knowledge too deep to spread around. undefined: none of the four fits, which is worth recording plainly rather than forcing a label that misleads.",
          "type": "string",
          "enum": [
            "stream-aligned",
            "platform",
            "enabling",
            "complicated-subsystem",
            "undefined"
          ]
        },
        "subtype": {
          "title": "Subtype",
          "description": "If type is 'undefined', use this field to specify what kind of team this is (e.g., project team, frontend team, backend team, working group, tiger team).",
          "type": "string",
          "enum": [
            "project",
            "frontend",
            "backend",
            "working group",
            "tiger",
            "other"
          ]
        },
        "description": {
          "title": "Team Description",
          "description": "What this team is for, and where its remit ends.",
          "type": "string",
          "maxLength": 2000
        },
        "size_band": {
          "title": "Size Band",
          "description": "Roughly how many people are on the team. Left unset, 5-9 is assumed: that is the band Team Topologies argues most teams should sit in.",
          "type": "string",
          "enum": [
            "1-4",
            "5-9",
            "10-15",
            "16-30",
            "31-50",
            "51-100",
            "101-150",
            "150+"
          ],
          "default": "5-9"
        },
        "stewards": {
          "title": "Stewards",
          "description": "What this team looks after: capabilities, technical systems, or both. Give a bare name, or a relationship object where the link needs a role or a label.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        },
        "interacts": {
          "title": "Interacts",
          "description": "How this team works with others, written inline here rather than in a section of its own.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/teamInteraction"
          }
        }
      }
    },
    "teamInteraction": {
      "title": "Team Interaction",
      "description": "One team's account of how it works with another. Declared on the team doing the describing.",
      "type": "object",
      "required": [
        "with",
        "mode"
      ],
      "additionalProperties": false,
      "properties": {
        "with": {
          "title": "Target Team",
          "description": "The team at the far end of this interaction, by name.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "mode": {
          "title": "Interaction Mode",
          "description": "Which of the three Team Topologies interaction modes this is. x-as-a-service: one side consumes what the other provides, with little conversation needed; collaboration: both sides work on it together and share the discovery; facilitating: one side helps the other get better at something, with an end in sight. undefined: none of the three describes it — a valid answer, and often the most useful one, because an interaction nobody can classify is usually the one worth examining.",
          "type": "string",
          "enum": [
            "x-as-a-service",
            "collaboration",
            "facilitating",
            "undefined"
          ]
        },
        "classification": {
          "title": "Interaction Classification",
          "description": "Classify this interaction to clarify its nature and implications. handover: work is passed from one team to another; temporal-coupling: teams must coordinate closely in time but can work asynchronously; ongoing-coordination: teams must coordinate closely and frequently; ongoing-facilitation: one team actively helps the other adopt a capability; ongoing-collaboration: two teams work closely together on an ongoing basis.",
          "type": "string",
          "enum": [
            "handover",
            "temporal-coupling",
            "ongoing-coordination",
            "ongoing-facilitation",
            "ongoing-collaboration"
          ]
        },
        "purpose": {
          "title": "Interaction Purpose",
          "description": "The primary purpose of this interaction. information-sharing: exchanging information or updates; coordination: coordinating work or schedules; support: providing help or expertise; decision-making: making joint decisions; innovation: collaborating on new ideas or improvements.",
          "type": "string",
          "maxLength": 500
        },
        "duration": {
          "title": "Interaction Duration",
          "description": "The typical duration of this interaction. short-term: one-off or infrequent interactions; medium-term: regular interactions that last a few weeks or months; long-term: ongoing interactions that last more than a few months.",
          "type": "string",
          "enum": [
            "short-term",
            "medium-term",
            "long-term"
          ]
        },
        "coupling_strength": {
          "title": "Coupling Strength",
          "description": "The strength of the coupling relationship between these teams. loose: occasional interactions with minimal coordination; moderate: regular interactions with some coordination; tight: frequent interactions with close coordination and collaboration.",
          "type": "string",
          "enum": [
            "loose",
            "moderate",
            "tight"
          ]
        },
        "health": {
          "title": "Interaction Health",
          "description": "The health of this interaction. neutral: no view recorded; healthy: interactions are smooth and effective; problematic: interactions have some friction or issues that need attention; unhealthy: interactions are dysfunctional and causing problems.",
          "type": "string",
          "enum": [
            "neutral",
            "healthy",
            "problematic",
            "unhealthy"
          ]
        },
        "via": {
          "title": "Via",
          "description": "A capability or technical system involved in this interaction.",
          "type": "string",
          "maxLength": 200
        },
        "description": {
          "title": "Description",
          "description": "A short phrase a reader would recognise, saying what actually passes between the two teams.",
          "type": "string",
          "maxLength": 500
        },
        "reviewHorizon": {
          "title": "Review Horizon",
          "description": "When should we review this interaction? The date at which this interaction should be reviewed for continued relevance and health. Set automatically by the platform based on the interaction mode and duration, but can be overridden by the user.",
          "type": "string",
          "format": "date-time"
        }
      }
    },
    "orgGroup": {
      "title": "Org Group",
      "description": "An optional grouping of teams.",
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Group Name",
          "description": "The name of the grouping. Must be unique within the model.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "type": {
          "title": "Group Type",
          "description": "The type of grouping. value-stream: end-to-end flow of work delivering value to customers; platform: provides internal services used by other teams; tribe/domain: collection of teams working in the same business domain; org-unit: any other kind of organizational grouping.",
          "type": "string",
          "enum": [
            "value-stream",
            "platform",
            "tribe/domain",
            "org-unit",
            "department",
            "other"
          ]
        },
        "size_band": {
          "title": "Size Band",
          "description": "Number of people in the group.",
          "type": "string",
          "enum": [
            "1-4",
            "5-9",
            "10-15",
            "16-30",
            "31-50",
            "51-100",
            "101-150",
            "150+"
          ]
        },
        "description": {
          "title": "Group Description",
          "description": "A description of what this group provides and what it classifies.",
          "type": "string",
          "maxLength": 2000
        },
        "contains": {
          "title": "Contains",
          "description": "Names of the teams or capabilities contained within this group.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/relationship"
          }
        }
      }
    },
    "externalDependency": {
      "title": "External Dependency",
      "description": "Something relied on from outside the modelled boundary. Declared here as a bare fact; the systems that call it say so themselves, under externalDeps.",
      "type": "object",
      "required": [
        "name",
        "description"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "External Dependency Name",
          "description": "What this outside system is called. Unique across external dependencies, and the exact string technical systems use in externalDeps.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "description": {
          "title": "External Dependency Description",
          "description": "What this outside system does for the organisation, and what would stop working without it.",
          "type": "string",
          "maxLength": 500
        }
      }
    },
    "dataAsset": {
      "title": "Data Asset",
      "description": "A store or channel shared by more than one technical system. Recording it makes visible the coupling that sharing creates, which a diagram of services alone will not show.",
      "type": "object",
      "required": [
        "name",
        "type",
        "description"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "title": "Data Asset Name",
          "description": "What this data asset is called. Names are unique across data assets, and technical systems are listed against them by name.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "type": {
          "title": "Data Asset Type",
          "description": "What kind of shared store or channel this is. database: a relational or document store; cache: a fast in-memory layer in front of something slower; event-stream: an append-only stream of messages; blob-storage: files and large objects; search-index: a queryable index built from data that lives elsewhere.",
          "type": "string",
          "enum": [
            "database",
            "cache",
            "event-stream",
            "blob-storage",
            "search-index"
          ]
        },
        "description": {
          "title": "Data Asset Description",
          "description": "What is actually held or carried here, in enough detail that a reader can see why two systems sharing it are coupled.",
          "type": "string",
          "maxLength": 500
        },
        "usedBy": {
          "title": "Used By",
          "description": "The technical systems that touch this asset. Declared here and only here: a technical system never lists the assets it uses.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          }
        }
      }
    },
    "relationship": {
      "title": "Relationship",
      "description": "A pointer from one entry to another. Two ways to write it: a plain string when the name is all there is to say, or an object when the link itself needs a label, a role or an identifier. Both forms may appear in the same list.",
      "oneOf": [
        {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "description": "Short form: just the name of the thing being pointed at."
        },
        {
          "type": "object",
          "required": [
            "target"
          ],
          "additionalProperties": false,
          "properties": {
            "target": {
              "title": "Target",
              "description": "The name of the thing at the far end of this link.",
              "type": "string",
              "minLength": 1,
              "maxLength": 200
            },
            "description": {
              "title": "Description",
              "description": "An optional label for the link itself, for where naming the two ends does not tell the whole story.",
              "type": "string",
              "maxLength": 500
            },
            "role": {
              "title": "Role",
              "description": "What part this link plays. primary: the main one; supporting: contributes without owning; consuming: uses it as a client. This says what part the relationship plays structurally. It is not a confidence level and must never be used to record how sure anyone is that the relationship exists.",
              "type": "string",
              "enum": [
                "primary",
                "supporting",
                "consuming"
              ]
            }
          }
        }
      ]
    }
  }
}
